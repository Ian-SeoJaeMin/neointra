<% include ./header %>
<div class="container body">
    <div class="main_container">
        <% include ../includes/sidemenu.ejs %>
        <% include ../includes/topnav.ejs %>

        <div class="right_col" role="main">
            <div class="">

                <div class="page-title">
                    <div class="title_left">
                        <h3> 홈페이지 관리 </h3>

                    </div>
                </div>

                <div class="clearfix"></div>
                <div class="row">
                    <% if (user['설정']['admin'] === 1 || user['설정']['보류매크로'] === 1) { %>
                    <% include ./options/holdmacro.ejs %>
                    <% } %>
                    <% if (user['설정']['admin'] === 1 || user['설정']['처리구분'] === 1) { %>
                    <% include ./options/process.ejs %>
                    <% } %>
                </div>
                <div class="row">
                    <% if (user['설정']['admin'] === 1 || user['설정']['확인내용'] === 1) { %>
                    <% include ./options/confirmForm.ejs %>
                    <% } %>
                    <% if (user['설정']['admin'] === 1 || user['설정']['우선순위'] === 1) { %>
                    <% include ./options/priority.ejs %>
                    <% } %>
                </div>

                <div class="row">
                    <% if (user['설정']['admin'] === 1 || user['설정']['AS담당자'] === 1) { %>
                    <% include ./options/serviceRotation.ejs %>
                    <% } %>
                </div>
                <div class="row">
                    <% if (user['설정']['admin'] === 1 || user['설정']['정산'] === 1) { %>
                    <% include ./options/memberCommission.ejs %>
                    <% } %>
                </div>
                <div class="row">
                    <% if (user['설정']['admin'] === 1 || user['설정']['게시판권한'] === 1) { %>
                    <% include ./options/boardPermission.ejs %>
                    <% } %>
                </div>
                <div class="row">
                    <% if (user['설정']['admin'] === 1 || user['설정']['권한관리'] === 1) { %>
                    <% include ./options/memberPermission.ejs %>
                    <% } %>
                </div>

            </div>
        </div>
    </div>
</div>

<% include ./footer %>
